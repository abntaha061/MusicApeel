# Add project-specific ProGuard rules here.
# By default, the ProGuard rules file is configured to keep all fields or methods 
# required for Room, Kotlin serialization, and other core packages.
-keep class androidx.room.concurrent.BundledSQLiteConnection { *; }
